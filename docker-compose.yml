name: obligatorio_final
services: 
  frontend_db:
    image: frontend_db
    container_name: frontend_db
    build:
      context: ./front
    command: sh -c "npm install && npm start"
    ports:
      - "4200:4200"
    volumes:
      - ./front/:/front
      